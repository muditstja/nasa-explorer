import { useEffect, useState } from 'react'; import { Observable, Subscription } from 'rxjs';
export function useObservable<T>(obs$: Observable<T>, initial: T){ const [value,setValue]=useState<T>(initial); useEffect(()=>{ const sub:Subscription = obs$.subscribe(setValue); return ()=>sub.unsubscribe() },[obs$]); return value }